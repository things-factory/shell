import * as ptr from '@things-factory/utils'

export function pulltorefresh(opts) {
  console.warn(`pulltorefresh is deprecated. please use @things-factory/utils's instead of @things-factory/shell's`)
  return ptr.pulltorefresh(opts)
}
